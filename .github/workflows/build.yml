name: Tiny CI Build

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Run tiny build
      run: |
        echo "Build started"
        cat hello.txt
        echo "Build finished successfully"
